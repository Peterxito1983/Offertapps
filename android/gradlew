#!/usr/bin/env sh
# Generated by Gradle Wrapper
# This script invokes the Gradle Wrapper to download the appropriate Gradle distribution.
# It expects the wrapper JAR at gradle/wrapper/gradle-wrapper.jar

# Resolve the location of the script
APP_HOME="$(cd "$(dirname "$0")" && pwd)"

# Execute the wrapper
java -jar "$APP_HOME/gradle/wrapper/gradle-wrapper.jar" "$@"
